# application.properties

spring.application.name=nexbuy

# ✅ Database Configuration
# Use Railway variables if available, otherwise fall back to local setup
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/nexbuy_db?createDatabaseIfNotExist=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:liki@30042003}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ✅ JWT Configuration (same as before)
jwt.secret=c6k5ePXng/ye4IPkMMMkYXDTgAQHowDkOXcENS6wLGQSeGPQsbGAr5AuHSDFtMMz
jwt.expiration=86400000

# ✅ JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true
spring.jpa.open-in-view=true
spring.jpa.properties.hibernate.jdbc.batch_size=20

# ✅ File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ✅ Server Port
server.port=${PORT:8080}

# ✅ CORS Configuration
cors.allowed.origins=http://localhost:3000,http://localhost:5173
